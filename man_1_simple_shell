.TH man 1 "15 December 2023" "1.0" "mishell The Shell man page"
.SH NAME
.B mishell
- A basic UNIX command line interpreter.
.SH SYNOPSIS
mishell can interpret and execute command line arguments read from the standard input.
.SH DESCRIPTION
mishell is a program that reads line by line from a file or the terminal line. It then interprets the lines and executes it if the line is a valid command.

.SH USAGE
All the files are to be compiled on an Ubuntu 14.04 LTS machine with "gcc -Wall -Werror -Wextra -pedantic *.c -o mishell".

Once compiled, to start the program, run:
.br
$ ./mishell

To exit the program, run:
.br
mishell $ exit

.SH BUILT-INS
The following built-ins are supported by the mishell shell:

.B env -
Print the current environment.

.B setenv VARIABLE VALUE -
Initialize a new environment variable, or modify an existing one.

.B unsetenv VARIABLE -
Remove an environment variable.

.SH RETURN VALUE
mishell shell will exit with status 0 unless status is specified.
.SH SEE ALSO
.I sh(1)
.SH BUGS
No known bugs.
.SH AUTHOR
Gilbert A. Henyo, Isaac Kumatse
